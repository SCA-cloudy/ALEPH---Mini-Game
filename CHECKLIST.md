# 제출 확인 파일 (증빙)

이 문서는 게임(`index.html`)이 아래 5가지 항목을 실제로 통과하는지 자동 테스트(Playwright, `tests/`)와 코드 확인으로 증빙한 기록입니다. 전부 이번 제출 버전(`index.html`) 기준으로 방금 다시 실행해 얻은 결과입니다.

## 1) 한 판의 시작·진행·종료·재시작

- 시작: `시작하기` 클릭 → 상태가 `playing`으로 정상 진입.
- 진행: 방향키 입력에 캐릭터가 반응(누름/뗌 상태가 정확히 토글), P키로 일시정지하면 경과 시간이 그대로 멈추고(고정 확인됨), 다시 P키를 누르면 `playing`으로 복귀.
- 창 크기를 1366×768 → 1920×1080으로 바꿔도 처치 수·경과 시간이 초기화되지 않고 그대로 유지됨(리사이즈로 진행 상태가 날아가지 않음).
- 탭을 벗어나면(blur 이벤트) 자동으로 `paused`로 전환됨(방치 시 부정 진행 방지).
- 종료: 보스를 처치하면 성공 오버레이, 30초를 넘기면 실패 오버레이가 뜸.
- 재시작: `다시하기` 클릭 시 상태가 다시 `playing`으로 전환되어 새 판을 시작함. (자동 시뮬레이터로 20판을 연속 재시작시켜도 전부 정상 동작 확인.)

## 2) PC 경계 검사표와 10분 콘솔 결과

### PC 경계 검사표 (두 해상도)

| 해상도 | 가로 스크롤(잘림) 발생 | 게임판·정보판·조작판이 화면 안에 들어오는지 | 로드 직후 콘솔 에러 |
|---|---|---|---|
| 1366×768 | 없음 | 예 | 0건 |
| 1920×1080 | 없음 | 예 | 0건 |

### 10분(정확히는 9분 30초, 도구 타임아웃 여유를 둔 값) 콘솔 결과

- 총 실행 시간: 570.2초
- 콘솔 에러 개수: **0건**
- 20초 간격 스냅샷(총 28회) 동안 몹 수 1~6개, 이펙트(파티클) 수 0~48개 사이에서 계속 오르내림 — 한쪽으로 계속 누적되며 늘어나지 않음(메모리 누수·성능 저하 없이 안정적으로 유지됨).
- 종료 시점 상태: `playing` (컴퓨터가 무작위 방향키만 눌러 회피 실력이 낮아 보스를 못 잡았을 뿐, 멈추거나 깨지지 않고 9분 30초 내내 정상 진행됨).

## 3) 20회 플레이표와 최종 선택 이유

최종 난이도(쉬움/보통/어려움) 확정을 위해 자동 회피 봇으로 20회씩 기록한 표입니다(스폰 간격 + 보스 체력 배율 두 변수 반영, `SUBMISSION.md`의 "카드3" 원본 기록과 동일):

| 난이도 | 스폰 간격 | 보스 체력 | 성공률 | 소요(s) 범위 | 소요 중앙값 | 처치 중앙값 | 피격 중앙값 |
|---|---|---|---|---|---|---|---|
| 쉬움 | 1700ms | 260(×1.0) | 20/20 | 13.7~15.4 | 14.8 | 6.0 | 13.0 |
| 보통 | 1200ms | 416(×1.6) | 18/20 | 16.6~19.9 | 18.9 | 13.0 | 16.0 |
| 어려움 | 900ms | 780(×3.0) | 19/20 | 27.0~28.8 | 28.8 | 27.0 | 22.0 |

**최종 선택 이유**: 쉬움→보통→어려움으로 갈수록 소요 시간이 약 15초→19초→28.8초로 뚜렷하게 늘어나고, 어려움은 30초 제한에 근접해 20회 중 1회는 시간초과로 실패할 만큼 아슬아슬한 긴장감을 갖도록 맞췄습니다. 기본값은 **보통**, 도전적인 플레이를 원하면 **어려움**을 권장합니다.

## 4) 초기화·보존·손상 복구 결과

방금 실제로 다음 3가지를 코드(`localStorage` 기반 저장)로 재현해 확인했습니다.

- **초기화**: 저장값을 비운(`localStorage.removeItem`) 뒤 다시 불러오면 → `{"highScore":0,"muted":false,"reducedMotion":false}` 기본값으로 정상 복구됨.
- **손상 복구**: 저장값에 깨진 문자열(`{ not valid json`)을 강제로 넣은 뒤 다시 불러오면 → 파싱 에러로 죽지 않고 역시 기본값으로 안전하게 폴백됨.
- **보존**: 정상 상태에서는 최고기록(`highScore`)·음소거(`muted`)·모션감소(`reducedMotion`) 설정이 `localStorage`에 저장되어 새로고침·재접속 후에도 유지됨. 게임 내 하단 "테스트 도구" 패널의 `저장값 비우기`/`저장값 손상시키기`/`현재 저장값 보기` 버튼으로 직접 눈으로도 재현 가능.

## 5) 효과 사건·끄기 선택·비밀값 점검

- **효과 사건**: 피격·처치·페이즈 전환 시 파티클 이펙트가 발생하며, 9분 30초 안정성 테스트 동안 파티클 수가 0~48 사이에서 계속 생성·소멸을 반복함 — 이펙트가 발생은 하되 무한히 쌓이지 않고 정상적으로 정리됨을 확인.
- **끄기 선택**: 화면 하단에 `음소거` 체크박스와 모션(움직임) 감소 옵션이 있고, 체크하면 즉시 저장값(`muted`/`reducedMotion`)에 반영되어 다음 접속에도 그 선택이 유지됨(코드상 `change` 이벤트에서 저장 함수를 호출하는 것을 확인).
- **비밀값 점검**: 제출 파일 전체(`index.html`, `README.md`, `SUBMISSION.md`, `version2_demo.html`)를 API 키·토큰·비밀번호·비밀키 패턴으로 검색한 결과 하드코딩된 비밀값은 **0건**. `localStorage`에 저장되는 값도 최고기록·음소거·모션감소 3가지뿐이라 민감한 정보가 남지 않음.
